import { answerNumber } from 'quora-clone/helpers/answer-number';
import { module, test } from 'qunit';

module('Unit | Helper | answer number');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = answerNumber([42]);
  assert.ok(result);
});
